# Demo 素材说明

这是脱敏示例，不包含真实头像、参考音频或外部图片。

运行真实项目时，请准备：

- 参考音频：用于 TTS 音色参考，例如 `local-assets/ref_voice.wav`
- 数字人头像：用于 SadTalker，例如 `local-assets/avatar.jpg`
- HTML：本目录的 `index.html` 可作为结构示例
- STYLE_PLAN：本目录的 `STYLE_PLAN.yaml` 可作为字段示例

媒体素材不应提交到仓库。建议放在被 `.gitignore` 排除的本地目录中。
